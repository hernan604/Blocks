# rename this file to blocks.yml and put a ':' after 'name' if
# you want to use YAML like in old versions of Catalyst
name Blocks

Boot done

<Model::Blocks>
    schema_class Blocks::Schema
    <connect_info>
    dsn "dbi:mysql:database_name:hostname"
    user 
    password 
    <options>
      AutoCommit 1
    </options>
    loglevel 1
    </connect_info>
</Model::Blocks>
